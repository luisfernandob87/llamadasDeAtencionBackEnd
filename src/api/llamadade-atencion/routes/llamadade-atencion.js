'use strict';

/**
 * llamadade-atencion router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::llamadade-atencion.llamadade-atencion');
